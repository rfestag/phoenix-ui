import overlays from "@phoenix-ui/map/lib/overlays";
import baseLayers from "@phoenix-ui/map/lib/baseLayers";
const userLayers = [];

export default {
  center: [0, 0],
  zoom: 3,
  viewport: null,
  baseLayers,
  overlays,
  userLayers,
};
